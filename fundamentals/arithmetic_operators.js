const [a, b, c, d] = [3 ,5 ,1 ,15];

const sum = a + b + c + d; // in fix operator
const sub = d - b;
const multiply = a * b;
const div = d / a;
const modulus = a % 2;

let ex = 10;

ex++; // post fixed operextor
++ex; // pre fixed operatorex
console.log(sum, sub, multiply, div, modulus);